---
description: Protect an Elephant session from automatic cleanup
argument-hint: "[memory-id]"
disable-model-invocation: true
---

Call `elephant_command` with action `pin`, arguments `$ARGUMENTS`, and cwd
`${CLAUDE_PROJECT_DIR}`. Return the protected session ID.
