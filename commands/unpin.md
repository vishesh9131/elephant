---
description: Allow a protected Elephant session to be cleaned
argument-hint: [memory-id]
disable-model-invocation: true
---

Call `elephant_command` with action `unpin`, arguments `$ARGUMENTS`, and cwd
`${CLAUDE_PROJECT_DIR}`. Return the affected session ID.
