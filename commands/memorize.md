---
description: Force-save the current coding session into Elephant memory
disable-model-invocation: true
---

Call `elephant_command` with action `memorize`, cwd `${CLAUDE_PROJECT_DIR}`,
harness `claude-code`, and session_id `${CLAUDE_SESSION_ID}`. Return its message.
