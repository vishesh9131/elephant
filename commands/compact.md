---
description: Repack the Elephant database and reclaim unused space
disable-model-invocation: true
---

Call `elephant_command` with action `compact` and cwd `${CLAUDE_PROJECT_DIR}`.
Explain that it repacks storage without deleting logical session data.
