---
description: Recover an Elephant memory and continue the unfinished coding task
argument-hint: "[memory-id]"
disable-model-invocation: true
---

Call `elephant_command` with action `resume`, arguments `$ARGUMENTS`, cwd
`${CLAUDE_PROJECT_DIR}`, harness `claude-code`, and session_id
`${CLAUDE_SESSION_ID}`. Compare the returned capsule with the live worktree,
briefly identify the inherited objective, then immediately continue the work.
