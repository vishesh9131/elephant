---
description: Preview an Elephant memory without resuming it
argument-hint: "[memory-id]"
disable-model-invocation: true
---

Call `elephant_command` with action `peek`, arguments `$ARGUMENTS`, cwd
`${CLAUDE_PROJECT_DIR}`, and harness `claude-code`. Return its message without
continuing the task.
