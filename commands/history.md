---
description: List recent Elephant memories for this project
argument-hint: [limit]
disable-model-invocation: true
---

Call `elephant_command` with action `history`, arguments `$ARGUMENTS`, and cwd
`${CLAUDE_PROJECT_DIR}`. Return its message.
