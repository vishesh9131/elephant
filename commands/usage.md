---
description: Show Elephant database and transcript disk usage
disable-model-invocation: true
---

Call `elephant_command` with action `usage` and cwd `${CLAUDE_PROJECT_DIR}`.
Return its storage report.
