---
description: Show Elephant memory freshness and capture coverage
disable-model-invocation: true
---

Call `elephant_command` with action `status` and cwd `${CLAUDE_PROJECT_DIR}`.
Return its message.
