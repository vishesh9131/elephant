{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "memory": {
      "type": "stdio",
      "command": "python3",
      "args": ["${PLUGIN_ROOT}/mcp/server.py"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
