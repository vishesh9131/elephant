# Elephant continuity

When the user asks Elephant to resume previous work, use Elephant's recovery
tool for the current workspace. Compare the recovered Git state with the live
worktree and continue without repeating completed work. Never invent a previous
session and never ask the user to run a terminal command for Elephant.
